div(ng-include='"components/navbar/navbar.html"')

.container
  .row
    .col-lg-12
      h1.page-header Pages:
  .row
    .page.col-lg-12(ng-repeat='page in pagesCtrl.pages')
      .title
        a(href='#', tooltip='{{page.readableTitle}}') {{page.readableTitle}}
      .created {{page.created}}
      .content(ng-bind-html='page.content')
